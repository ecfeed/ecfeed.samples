package pages.elements.patterns;

import org.openqa.selenium.WebElement;

public class RadioElement extends AbstractWebElement{

	public RadioElement(WebElement element){
		super(element);
	}

}
