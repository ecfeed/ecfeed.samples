First, copy the 'security.p12. file and run following commands:
1) openssl pkcs12 -in security.p12 -out client.pem -clcerts -nokeys 
2) openssl pkcs12 -in security.p12 -out key.pem -nocerts
To run the code you must install Node.js first (https://nodejs.org/en/download/)

npm install
node index.js
