package pages.elements.patterns;

import org.openqa.selenium.WebElement;

public class ComboElement extends AbstractWebElement{

	public ComboElement(WebElement element){
		super(element);
	}

}
